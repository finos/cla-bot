node_modules
build
npm-debug.log
package-lock.json

# don't commit the file that simulates the AWS environment variables
deploy.env
.env
.lambda/

# I don't want to commit the private RSA keys for my bot!
# see: https://docs.travis-ci.com/user/encrypting-files/#Encrypting-multiple-files
*.pem
clabot-dev-integration-key.pem
clabot-integration-key.pem
secrets.tar
